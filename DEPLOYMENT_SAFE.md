# 🛡️ Безопасное развертывание на сервере с сохранением данных

## ⚠️ ВАЖНО: Перед обновлением

Это обновление добавляет поддержку второго Marzban сервера и новое поле `subscriptionUrl2` в таблицу `Subscription`. Все существующие данные будут сохранены.

## 📋 Пошаговая инструкция

### Способ 1: Автоматическое обновление (рекомендуется)

Используйте обновленный скрипт `update-server.sh`, который автоматически:
- Создаст бэкап базы данных
- Применит миграции
- Обновит код и зависимости
- Перезапустит бота

```bash
# Сделайте скрипт исполняемым (если еще не сделано)
chmod +x /opt/bot-marzban-vpn/update-server.sh

# Запустите обновление
/opt/bot-marzban-vpn/update-server.sh
```

### Способ 2: Ручное обновление (для контроля каждого шага)

#### Шаг 1: Подключитесь к серверу
```bash
ssh user@your-server
cd /opt/bot-marzban-vpn
```

#### Шаг 2: Остановите бота
```bash
pm2 stop bot-marzban-vpn
```

#### Шаг 3: Создайте резервную копию базы данных
```bash
# Создайте директорию для бэкапов (если её нет)
mkdir -p ./back

# Создайте бэкап с timestamp
cp ./prisma/dev.db ./back/backup_$(date +%Y%m%d_%H%M%S).db

# Проверьте, что бэкап создан
ls -lh ./back/
```

#### Шаг 4: Получите обновления из Git
```bash
git pull origin main
```

#### Шаг 5: Установите зависимости (если нужно)
```bash
npm install
```

#### Шаг 6: Примените миграции базы данных
```bash
# Важно: используйте migrate deploy для продакшена
npx prisma migrate deploy

# Это применит миграцию 20260116192259_add_subscription_url2
# Которая добавляет поле subscriptionUrl2 в таблицу Subscription
```

#### Шаг 7: Сгенерируйте Prisma Client
```bash
npx prisma generate
```

#### Шаг 8: Настройте переменные окружения (если нужно)

Добавьте в файл `.env` новые переменные для второго Marzban сервера:

```env
# Основной Marzban API (уже должен быть настроен)
MARZBAN_API_URL=https://your-main-marzban.com
MARZBAN_TOKEN=your_token

# Второй Marzban API (новое - добавьте эти строки)
MARZBAN_API_URL_2=http://51.250.72.185:3033
MARZBAN_TOKEN_2=your_token_2  # Если не указан, будет использован MARZBAN_TOKEN
```

#### Шаг 9: Перезапустите бота
```bash
pm2 restart bot-marzban-vpn
# или если бот не был запущен:
pm2 start index.js --name "bot-marzban-vpn"
```

#### Шаг 10: Проверьте статус
```bash
# Проверьте, что бот запущен
pm2 status

# Проверьте логи на наличие ошибок
pm2 logs bot-marzban-vpn --lines 50
```

## ✅ Проверка после обновления

1. **Проверьте, что бот отвечает:**
   - Откройте бота в Telegram
   - Отправьте команду `/start`
   - Убедитесь, что меню открывается

2. **Проверьте создание подписок:**
   - Купите тестовую подписку (M1)
   - Убедитесь, что вы получили **две ссылки**:
     - Основная ссылка: `https://vpn.grangy.ru/sub/...`
     - Ссылка для Миранда: `https://rus2.grangy.ru:8888/sub/...`

3. **Проверьте базу данных:**
   ```bash
   # Подключитесь к БД (опционально, для проверки)
   sqlite3 ./prisma/dev.db "SELECT id, type, subscriptionUrl, subscriptionUrl2 FROM Subscription LIMIT 5;"
   ```

## 🔄 Откат изменений (если что-то пошло не так)

Если после обновления возникли проблемы:

```bash
# 1. Остановите бота
pm2 stop bot-marzban-vpn

# 2. Восстановите базу данных из бэкапа
# Найдите последний бэкап
ls -lt ./back/backup_*.db | head -1

# Скопируйте бэкап обратно (замените имя файла на актуальное)
cp ./back/backup_YYYYMMDD_HHMMSS.db ./prisma/dev.db

# 3. Откатите код к предыдущей версии
git reset --hard HEAD~1

# 4. Перезапустите бота
pm2 start index.js --name "bot-marzban-vpn"
```

## 📝 Что изменилось в этой версии

- ✅ Добавлено поле `subscriptionUrl2` в таблицу `Subscription`
- ✅ Создан модуль `marzban-utils.js` для работы с двумя серверами
- ✅ При покупке подписки пользователь создается на обоих Marzban серверах
- ✅ При активации промокода пользователь создается на обоих серверах
- ✅ В сообщениях отображаются обе ссылки подписки

## ⚡ Важные замечания

1. **Миграция безопасна**: добавление поля `subscriptionUrl2` не удалит существующие данные
2. **Обратная совместимость**: старые подписки продолжат работать (у них просто не будет второй ссылки)
3. **Новые подписки**: все новые подписки будут создаваться с двумя ссылками
4. **Переменные окружения**: если `MARZBAN_API_URL_2` не настроен, вторая ссылка просто не будет создаваться (без ошибок)

## 🆘 Если возникли проблемы

1. Проверьте логи: `pm2 logs bot-marzban-vpn`
2. Проверьте, что все переменные окружения настроены: `cat .env | grep MARZBAN`
3. Убедитесь, что миграции применены: `npx prisma migrate status`
4. Проверьте доступность Marzban API: `curl -I $MARZBAN_API_URL_2`

## 📞 Контакты для поддержки

Если возникли проблемы, проверьте:
- Логи бота: `pm2 logs bot-marzban-vpn`
- Статус миграций: `npx prisma migrate status`
- Доступность API: проверьте URL второго Marzban сервера
