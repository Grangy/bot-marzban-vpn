# Инструкция по безопасному обновлению системы промокодов

## ⚠️ ВАЖНО: Создайте резервную копию БД перед обновлением!

Эта инструкция поможет безопасно обновить систему промокодов на сервере без потери данных.

## 📋 Подготовка

### 1. Остановите бота

```bash
cd /opt/bot-marzban-vpn
pm2 stop bot-marzban-vpn
```

### 2. Создайте резервную копию базы данных

```bash
# Создайте директорию для бэкапов (если её нет)
mkdir -p ./back

# Создайте бэкап с текущей датой и временем
cp prisma/dev.db ./back/dev.db.backup-$(date +%Y%m%d_%H%M%S).db

# Проверьте, что бэкап создан
ls -lh ./back/dev.db.backup-*.db | tail -1
```

**Важно:** Убедитесь, что бэкап создан и имеет ненулевой размер!

## 🔄 Обновление кода

### 3. Получите обновления из Git

```bash
cd /opt/bot-marzban-vpn
git pull origin main
```

### 4. Установите зависимости (если нужно)

```bash
npm install
```

## 🗄️ Обновление базы данных

### 5. Примените миграцию

**ВАЖНО:** Миграция безопасна и не удалит существующие данные. Она:
- Добавляет новые поля в таблицу `AdminPromo` (type, days, isReusable, customName, useCount)
- Сохраняет все существующие промокоды (они будут типа BALANCE)
- Создает новую таблицу `AdminPromoActivation` для отслеживания активаций

#### Вариант A: Автоматический скрипт (рекомендуется)

```bash
# Используйте готовый скрипт, который автоматически:
# - Создаст бэкап
# - Сделает baseline существующих миграций
# - Применит только новую миграцию
# - Проверит результат

bash apply-promo-migration-safe.sh
```

#### Вариант B: Ручное применение

Если вы получили ошибку **P3005** ("The database schema is not empty"), выполните:

```bash
# 1. Сделайте baseline для всех существующих миграций
npx prisma migrate resolve --applied 20250920090156_init
npx prisma migrate resolve --applied 20250920090822_add_subscription
npx prisma migrate resolve --applied 20250920091722_add_subscription2
npx prisma migrate resolve --applied 20250920092239_add_balance_subs_enum_topups
npx prisma migrate resolve --applied 20250920095543_add_subscription_url
npx prisma migrate resolve --applied 20250920114234_update_topup
npx prisma migrate resolve --applied 20250920115006_update_topup
npx prisma migrate resolve --applied 20250920120742_add_topup_credited_fields
npx prisma migrate resolve --applied 20250922192610_add_promocodes
npx prisma migrate resolve --applied 20250925142719_add_subscription_reminders
npx prisma migrate resolve --applied 20260116192259_add_subscription_url2

# 2. Примените новую миграцию
npx prisma migrate deploy
```

**Если возникнет ошибка "database is locked":**
```bash
# Остановите все процессы
pm2 stop all

# Подождите 5 секунд
sleep 5

# Повторите команду
npx prisma migrate deploy
```

### 6. Сгенерируйте Prisma Client

```bash
npx prisma generate
```

### 7. Проверьте схему базы данных

```bash
# Проверьте, что таблица AdminPromoActivation создана
sqlite3 prisma/dev.db "SELECT name FROM sqlite_master WHERE type='table' AND name='AdminPromoActivation';"

# Должно вернуть: AdminPromoActivation

# Проверьте структуру AdminPromo
sqlite3 prisma/dev.db "PRAGMA table_info(AdminPromo);"

# Должны быть поля: type, days, isReusable, customName, useCount
```

## 🚀 Запуск бота

### 8. Запустите бота

```bash
pm2 start bot-marzban-vpn
# или если бот уже был запущен:
pm2 restart bot-marzban-vpn
```

### 9. Проверьте логи

```bash
# Просмотрите последние логи
pm2 logs bot-marzban-vpn --lines 50

# Проверьте, что нет ошибок
# Должны быть сообщения о успешном запуске модулей
```

## ✅ Проверка работоспособности

### 10. Проверьте работу промокодов

1. **Создайте тестовый промокод на баланс:**
   ```
   /createpromo 100
   ```

2. **Создайте тестовый промокод на дни:**
   ```
   /createpromo days 7 Тестовый
   ```

3. **Создайте многоразовый промокод:**
   ```
   /createpromo days 30 Блогер --reusable
   ```

4. **Проверьте список промокодов:**
   ```
   /promos
   ```

5. **Проверьте активацию через бота:**
   - Откройте бота в Telegram
   - Введите `/promo <код_промокода>`
   - Убедитесь, что промокод активируется

6. **Проверьте API:**
   ```bash
   curl -X POST http://localhost:4000/api/promo/activate \
     -H "Content-Type: application/json" \
     -d '{"telegramId": "YOUR_TELEGRAM_ID", "code": "TEST_CODE"}'
   ```

## 🔙 Откат (если что-то пошло не так)

Если после обновления возникли проблемы:

### 1. Остановите бота

```bash
pm2 stop bot-marzban-vpn
```

### 2. Восстановите базу данных из бэкапа

```bash
# Найдите последний бэкап
ls -lt ./back/dev.db.backup-*.db | head -1

# Скопируйте бэкап обратно (замените YYYYMMDD_HHMMSS на реальную дату)
cp ./back/dev.db.backup-YYYYMMDD_HHMMSS.db prisma/dev.db

# Проверьте, что БД восстановлена
sqlite3 prisma/dev.db "SELECT COUNT(*) FROM AdminPromo;"
```

### 3. Откатите код Git

```bash
# Откатитесь на предыдущий коммит
git log --oneline -5  # Посмотрите историю коммитов
git reset --hard <PREVIOUS_COMMIT_HASH>

# Или просто откатите последний коммит
git reset --hard HEAD~1
```

### 4. Регенерируйте Prisma Client

```bash
npx prisma generate
```

### 5. Запустите бота

```bash
pm2 start bot-marzban-vpn
```

## 📊 Что изменилось

### Новые возможности:

1. **Многоразовые промокоды** - один промокод могут активировать разные пользователи
2. **Промокоды на дни** - можно создавать промокоды, дающие подписку на указанное количество дней
3. **Кастомные названия** - для промокодов на дни можно указать название
4. **Улучшенная система активации:**
   - Один пользователь может активировать реферальный промокод только 1 раз
   - Один пользователь может активировать один и тот же админский промокод только 1 раз
   - Разные админские промокоды можно активировать сколько угодно раз

### Новые команды:

- `/createpromo <сумма>` - промокод на баланс
- `/createpromo days <дни> [название] [--reusable]` - промокод на дни
- `/promos` - список всех активных промокодов (обновлен)

### API:

- Автоматическое определение типа промокода
- Поддержка всех типов промокодов (реферальные, админские на баланс, админские на дни)

## 🆘 Решение проблем

### Ошибка "database is locked"

```bash
# Остановите все процессы
pm2 stop all

# Подождите 5 секунд
sleep 5

# Повторите миграцию
npx prisma migrate deploy
```

### Ошибка "table AdminPromoActivation does not exist"

```bash
# Примените миграцию вручную
sqlite3 prisma/dev.db < prisma/migrations/20260120000000_add_advanced_promo_system/migration.sql

# Затем отметьте миграцию как примененную
npx prisma migrate resolve --applied 20260120000000_add_advanced_promo_system
```

### Ошибка "Invalid prisma.user.findMany() invocation"

```bash
# Регенерируйте Prisma Client
npx prisma generate

# Перезапустите бота
pm2 restart bot-marzban-vpn
```

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Логи бота: `pm2 logs bot-marzban-vpn`
2. Структуру БД: `sqlite3 prisma/dev.db ".schema AdminPromo"`
3. Примененные миграции: `npx prisma migrate status`

---

**Дата создания:** 2026-01-20  
**Версия:** 2.0.0
