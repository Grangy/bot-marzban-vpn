# Инструкция по настройке и использованию веб-интерфейса рассылок

## Описание

Веб-интерфейс для массовой рассылки сообщений пользователям Telegram бота MaxGroot. Поддерживает:
- ✅ HTML форматирование текста
- ✅ Эмодзи (emoji)
- ✅ Кнопку для открытия Web App
- ✅ 3 варианта отправки: конкретному пользователю, активным, всем

## Установка на сервере

### 1. Обновление кода

```bash
cd /opt/bot-marzban-vpn
git pull
npm install
```

### 2. Настройка переменных окружения

Добавьте в `.env` файл:

```bash
# Секретный ключ для доступа к админ-панели рассылок
ADMIN_BROADCAST_SECRET=ваш_секретный_ключ_здесь
```

**Важно:** Используйте сложный случайный ключ. Например:
```bash
ADMIN_BROADCAST_SECRET=$(openssl rand -hex 32)
```

### 3. Создание папки для статических файлов

```bash
mkdir -p /opt/bot-marzban-vpn/public
```

Файл `broadcast.html` уже должен быть в этой папке после `git pull`.

### 4. Настройка nginx (если используется)

Добавьте в конфиг nginx для `grangy.ru`:

```nginx
server {
    listen 443 ssl;
    server_name grangy.ru;
    
    # ... существующие настройки SSL ...
    
    # Веб-интерфейс рассылок
    location /broadcast.html {
        proxy_pass http://127.0.0.1:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # API рассылок
    location /admin/broadcast/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # ... остальные location ...
}
```

Проверьте конфиг и перезагрузите nginx:

```bash
nginx -t
systemctl reload nginx
```

### 5. Перезапуск бота

```bash
pm2 restart bot-marzban-vpn
```

## Использование

### Доступ к интерфейсу

Откройте в браузере:
```
https://grangy.ru/broadcast.html
```

При открытии вас попросят ввести секретный ключ (из `.env` файла).

### Варианты рассылки

#### 1. Конкретному пользователю
- Выберите "📱 Конкретный пользователь"
- Введите Telegram ID пользователя (например: `683203214`)
- Введите текст сообщения
- Нажмите "Отправить"

#### 2. Активным пользователям
- Выберите "✅ Активные подписки"
- Введите текст сообщения
- Отправит всем пользователям с активной (не истекшей) подпиской

#### 3. Всем пользователям
- Выберите "🌍 Все пользователи"
- Введите текст сообщения
- Отправит всем пользователям из личных сообщений

### Форматирование текста

#### HTML теги:
- `<b>жирный текст</b>` - жирный
- `<i>курсив</i>` - курсив
- `<code>код</code>` - моноширинный
- `<a href="https://example.com">ссылка</a>` - ссылка
- `<u>подчеркнутый</u>` - подчеркнутый
- `<s>зачеркнутый</s>` - зачеркнутый

#### Эмодзи:
Эмодзи можно вставлять напрямую: 🚀 ✅ 💙 👍 ❤️ 🎉

Или использовать HTML entities:
- `&#128640;` = 🚀
- `&#9989;` = ✅
- `&#128154;` = 💙

#### Переносы строк:
Просто нажмите Enter для новой строки.

### Пример сообщения

```
Сегодня у MaxGroot большое обновление! 🚀

🎉 <b>Новое мини-приложение</b> внутри чат-бота  

Теперь оформление подписки стало ещё удобнее.  

• можно оформить подписку в пару нажатий 💳
• есть удобные инструкции 📲💻

<a href="https://t.me/vpnmax_off">Следите за новостями</a> 📌

Спасибо, что пользуетесь MaxGroot! 💙
```

### Кнопка "Открыть приложение"

Если нужно добавить кнопку для открытия Web App:
- ✅ Отметьте чекбокс "Добавить кнопку '📱 Открыть приложение'"
- Кнопка будет добавлена под сообщением и откроет `https://web.grangy.ru/`

### Предпросмотр

Перед отправкой можно использовать кнопку "👁️ Предпросмотр" для проверки сообщения.

## Безопасность

### Важно:

1. **Секретный ключ** (`ADMIN_BROADCAST_SECRET`) должен быть сложным и случайным
2. Не делитесь секретным ключом
3. Используйте HTTPS для доступа к интерфейсу
4. В продакшене лучше ограничить доступ по IP адресу в nginx:

```nginx
location /broadcast.html {
    allow 192.168.1.100;  # Ваш IP
    deny all;
    proxy_pass http://127.0.0.1:4000;
}
```

## Отчет о рассылке

После отправки вы увидите отчет:
- ✅ Всего получателей
- ✅ Успешно отправлено
- ❌ Количество ошибок
- 📋 Список ошибок (первые 10)

## Ограничения

- **Лимиты Telegram**: ~30 сообщений в секунду
- Скрипт автоматически добавляет задержку 50мс между сообщениями
- Рассылка отправляет только пользователям из **личных сообщений** (не из групп)

## Устранение проблем

### Ошибка "UNAUTHORIZED"
- Проверьте, что `ADMIN_BROADCAST_SECRET` установлен в `.env`
- Проверьте, что вы вводите правильный ключ при открытии интерфейса
- Перезапустите бота после изменения `.env`

### Сообщения не отправляются
- Проверьте логи бота: `pm2 logs bot-marzban-vpn`
- Убедитесь, что бот запущен: `pm2 status`
- Проверьте, что пользователь не заблокировал бота

### Страница не открывается
- Проверьте, что nginx настроен правильно
- Проверьте, что файл `broadcast.html` существует в папке `public/`
- Проверьте логи nginx: `tail -f /var/log/nginx/error.log`

## API Endpoints

Для программистов - доступны следующие endpoints:

### POST /admin/broadcast/send
Отправить рассылку

**Headers:**
```
X-Admin-Secret: ваш_секретный_ключ
Content-Type: application/json
```

**Body:**
```json
{
  "type": "single" | "active" | "all",
  "message": "Текст сообщения",
  "telegramId": "683203214",  // только для type="single"
  "parseMode": "HTML",
  "hasWebAppButton": true
}
```

### GET /admin/broadcast/stats
Получить статистику пользователей

**Headers:**
```
X-Admin-Secret: ваш_секретный_ключ
```

**Response:**
```json
{
  "ok": true,
  "stats": {
    "totalUsers": 100,
    "activeUsers": 50,
    "totalActiveSubscriptions": 60
  }
}
```

## Примеры использования через curl

### Проверка статистики:
```bash
curl https://grangy.ru/admin/broadcast/stats \
  -H "X-Admin-Secret: ваш_секретный_ключ"
```

### Отправка сообщения активным пользователям:
```bash
curl -X POST https://grangy.ru/admin/broadcast/send \
  -H "Content-Type: application/json" \
  -H "X-Admin-Secret: ваш_секретный_ключ" \
  -d '{
    "type": "active",
    "message": "Тестовое сообщение 🚀",
    "parseMode": "HTML",
    "hasWebAppButton": false
  }'
```

## Поддержка

При возникновении проблем обращайтесь к разработчику или проверьте логи:
```bash
pm2 logs bot-marzban-vpn --lines 50
tail -f /var/log/nginx/error.log
```
